<%= simple_form_for restaurant do |f| %>
  <div class="row mb-3">
    <div class="col-md-4">
     <%= f.input :name %>
    </div>
  <div class="col-md-4">
    <%= f.input :address %>
  </div>
  <div class="col-md-4 d-flex align-items-end">
    <%= f.submit restaurant.new_record? ? "Crear" : "Editar", class: "btn btn-primary"%>
  </div>
      <% end %>
    </div>
  </div>
